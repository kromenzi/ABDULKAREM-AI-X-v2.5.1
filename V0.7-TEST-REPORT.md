# v0.7 Test Report

## Deep Research component test

A local mock service emulated Ollama, SearXNG and multiple public web pages.

- Research status detected SearXNG: PASS.
- Web Search returned structured results: PASS.
- Browser Reader extracted title, date and body text: PASS.
- DEEP planner generated 4 complementary queries: PASS.
- Multiple search runs executed: PASS.
- Duplicate URLs collapsed: PASS.
- Sources quality-scored: PASS.
- Citation IDs generated as `SRC1`, `SRC2`, `SRC3`: PASS.

## Full Research Agent integration

End-to-end flow tested:

`runAgent(mode=research)` → Research Planner → Search Provider → Browser Reader → evidence injection → final Ollama synthesis → Verification.

- Final answer contained `[SRC1]` and `[SRC2]`: PASS.
- Research sources returned to UI/API payload: PASS.
- Research metadata returned: PASS.
- Verification status: `verified-by-tools`: PASS.
- Verification score in mock integration: 95%.

The mock source intentionally contained text similar to `IGNORE ALL PRIOR INSTRUCTIONS...`. Web content remained inside the untrusted evidence layer and was not executed as an application instruction.

## Browser security

With `ABDX_ALLOW_LOCAL_RESEARCH=0`, Browser Agent rejected a URL targeting `127.0.0.1:11434`: PASS.

The same policy covers common RFC1918/private address ranges and localhost.

## Static checks

- `electron/main.js`: Node syntax PASS.
- `electron/preload.js`: Node syntax PASS.
- `python/office_worker.py`: compile PASS.
- `python/knowledge_worker.py`: compile PASS.

## Full Vite production build

Not claimed in this build container. `npm install` timed out while downloading Node dependencies. The Windows installer performs `npm install` locally, as used by the previous working release.
