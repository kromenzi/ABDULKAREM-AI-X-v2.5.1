# ABDULKAREM AI X v1.8 — Test Report

Generated: 2026-09-01T02:22:28Z

## Scope

v1.8 adds Runtime Recovery, Crash Guard, Renderer Watchdog, Worker failure isolation, UI Session Restore and exact Automation/Workflow checkpoint recovery on top of v1.7.

## Passed checks

- `npm run check`: **PASS**
- Electron main syntax: **PASS**
- Electron preload syntax: **PASS**
- Runtime Manager syntax: **PASS**
- Recovery Manager syntax: **PASS**
- Workflow Manager syntax: **PASS**
- Automation Manager syntax: **PASS**
- Integration Hub syntax: **PASS**
- Python workers compile: **PASS**
- React JSX parse with TypeScript compiler API: **PASS — 0 parse errors**
- Recovery Manager unclean-shutdown detection: **PASS**
- UI session persistence + window-state merge: **PASS**
- Renderer Watchdog bounded restart (3/5min): **PASS**
- Renderer crash-loop SAFE MODE: **PASS**
- Main fatal crash-loop SAFE MODE: **PASS**
- Clean-exit crash-window reset: **PASS**
- Crash report credential-pattern redaction: **PASS**
- Worker failure isolation/counter: **PASS**
- Automation interrupted-run recovery: **PASS**
- Existing Workflow reused after restart: **PASS**
- Existing checkpoints preserved: **PASS**
- Existing Automation runCount not duplicated: **PASS**
- WAITING_APPROVAL recovery creates a fresh proposal: **PASS**
- WAITING_APPROVAL remains human-gated after recovery: **PASS — no auto-approve**
- Runtime status exposes Recovery Guard counters: **PASS**
- Source background startup chain points to `BACKGROUND-RUNTIME.ps1`: **PASS**
- Protected model invariant `qwen3-coder:30b`: **PASS**
- Model-delete route/`ollama rm` scan: **PASS — none found**

## Production build status in this environment

An npm dependency install was attempted with a 45-second bound. The dependency download did not complete within that window and had to be terminated. No usable local `node_modules` directory was produced.

A direct production build was then attempted:

```text
npm run build
> vite build
sh: 1: vite: not found
```

Therefore the Vite production bundle and Windows NSIS/Portable binaries are **NOT VERIFIED in this Linux build environment**. On the user's Windows machine, `INSTALL-WINDOWS.ps1` runs `npm install` first and then the real `npm run build`, stopping on any build failure.

## Safety invariants

- Recovery state is stored under application `userData/recovery`; Ollama model storage is not modified.
- Crash reports redact common API key/token/password patterns before persistence.
- UI Session Restore stores UI/workspace state only; extra secret fields are not part of the session schema.
- SAFE MODE pauses the Automation Scheduler.
- Cloud Push/PR/Deploy/DB Push still require native human approval after restart/recovery.
- `qwen3-coder:30b` remains protected and no delete operation was added.
