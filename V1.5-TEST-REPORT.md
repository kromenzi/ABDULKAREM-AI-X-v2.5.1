# ABDULKAREM AI X v1.5 — Test Report

Date: 2026-09-01

## Static/source validation

- `npm run check`: PASS
  - `electron/main.js`: PASS
  - `electron/preload.js`: PASS
  - `electron/integrations.js`: PASS
  - `electron/workflow-manager.js`: PASS
  - `python/office_worker.py`: PASS (`py_compile`)
  - `python/knowledge_worker.py`: PASS (`py_compile`)
  - `python/memory_worker.py`: PASS (`py_compile`)
- React `src/main.jsx` parsed with the TypeScript JSX parser: PASS, 0 parser diagnostics.

## Workflow Engine unit/integration logic

Tested with a temporary persisted workflow store and deterministic mock executors.

1. `quality_gate` workflow:
   - completed all 4 steps: PASS
   - generated 4 checkpoints: PASS
   - final cursor = 4: PASS
2. `coding_repair` failure/recovery:
   - mock Agent failure stopped at exact Agent step: PASS
   - workflow status became `FAILED`: PASS
   - Retry executed the failed Agent step again: PASS
   - previously completed Inspect step was not replayed: PASS
   - workflow then completed: PASS
3. `code_release_preview` approval boundary:
   - reached Integration Proposal step: PASS
   - workflow entered `WAITING_APPROVAL`: PASS
   - proposal ID linked to step: PASS
   - successful approval notification advanced cursor: PASS
   - Resume continued from next step: PASS
   - workflow completed without replaying prior stages: PASS
4. Approval-ticket renewal:
   - missing/expired approval step can be retried: PASS
   - old proposal ID cleared: PASS
   - fresh proposal ID generated: PASS
   - attempts incremented: PASS
5. Persistence/restart behavior:
   - completed workflows survived manager reload: PASS
   - interrupted `WAITING_APPROVAL` restored as `PAUSED`: PASS
   - interrupted approval step reset to `PENDING`: PASS
   - stale approval ID removed: PASS

## Safety invariants

- `qwen3-coder:30b` remains in `PROTECTED_OLLAMA_MODELS`: PASS
- Existing command guard still blocks deletion of protected Ollama model: PASS by source inspection.
- Integration HTTP approval/rejection remains blocked with `403 human_approval_required`: PASS by source inspection.
- Workflow execution does not contain a Cloud approval method: PASS.
- Workflow Cloud step creates only an Integration Hub proposal and enters `WAITING_APPROVAL`: PASS.
- Workflow persistence is included in Backup/Restore allow-list: PASS.

## Production dependency/build attempt

`npm install --ignore-scripts --no-audit --no-fund` was attempted in the build environment and timed out after 45 seconds due dependency/network availability. No `node_modules` directory was retained. Therefore:

- Vite production bundle in this environment: NOT VERIFIED
- Electron NSIS `.exe` in this environment: NOT BUILT

The Windows installer script still performs a real `npm install` followed by `npm run build` and stops on failure; it does not report a fake successful build.
