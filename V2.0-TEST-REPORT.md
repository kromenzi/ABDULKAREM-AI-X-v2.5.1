# ABDULKAREM AI X v2.0 — Test Report

Version: `2.0.0`  
Codename: **Production Intelligence Core**

## Scope

This report covers the source-level, deterministic, and regression checks executed in the artifact environment for v2.0. No real Ollama model inference, GitHub push, Vercel deployment, Supabase mutation, or Windows NSIS packaging is claimed by this report.

## Automated check command

```text
npm run check
```

Result: **PASS** (`exit 0`).

The check covers:

- Electron main/preload JavaScript syntax
- Production Intelligence Core syntax
- Resource Governor syntax and deterministic tests
- Runtime, Recovery, Integrations, Workflow, and Automation manager syntax
- Unified Planner deterministic unit tests
- Intelligence integration safety invariants
- JSX parse validation
- Python Office/Knowledge/Memory worker compilation

## Unified Planner verification

PASS:

- coding request routes to `coder` and coding model kind
- coding Workspace plan contains verification stage
- research request routes to `researcher`
- research plan includes `source-evidence`
- Office request routes to `office`
- image attachment routes to `vision`
- mixed research + coding request uses team strategy and includes both specialists
- cloud deployment request includes reviewer + verifier and a human approval gate
- role-specific assignments are generated for specialists/reviewer/verifier
- Task Graph contains explicit dependency edges and final synthesis

The graph is a planning/execution-contract graph in v2.0. Agent specialist execution is still sequential; parallel execution is intentionally not claimed.

## Self-Evaluation and Verification Gates

PASS:

- weak coding result with insufficient execution evidence returns `PARTIAL`
- strong coding result with tool/file evidence clears required gates
- verification gate failures reduce the evaluation score
- source/artifact/cloud/final verification gates are selected from task signals

## Persistence

PASS:

- recent Planner history is persisted to `intelligence/state.json`
- a reloaded Intelligence Core reads persisted plan statistics/history
- intelligence state is included in application backup/restore allowlists

## Resource Governor regression

PASS:

```json
{
  "normalCtx": 8192,
  "pressureBlocked": true,
  "maxHeavy": 1,
  "oomCooldowns": 1
}
```

This confirms the existing v1.9 OOM-preflight and heavy-model exclusivity regressions remain covered.

## Security and invariants

PASS:

- `qwen3-coder:30b` remains in the protected-model set
- no `ollama rm` execution path exists in active application code
- no agent tool named `integration_approve` is exposed
- cloud approval/rejection remains native UI-only
- the Production Intelligence Capability Registry declares that agents cannot approve cloud mutations
- protected model deletion capability remains false

## JSX / Python

PASS:

```text
React JSX parser: 0 errors
Python workers: compile PASS
```

## Production build attempt

Command:

```text
npm run build
```

Result:

```text
vite: not found
```

Status: **NOT VERIFIED in this artifact environment** because dependencies are not installed here. This is not recorded as a successful Vite or Windows EXE build. The Windows installer script performs dependency installation before the production build.

## Overall result

**SOURCE / DETERMINISTIC REGRESSION: PASS**  
**PRODUCTION VITE BUILD: NOT VERIFIED HERE**  
**WINDOWS NSIS EXE: NOT BUILT HERE**
