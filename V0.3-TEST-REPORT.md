# v0.3 Test Report

## Passed

1. `node --check electron/main.js`
2. `node --check electron/preload.js`
3. `python -m py_compile python/office_worker.py`
4. Python worker functional file inspection.
5. API Gateway runtime boot using an Electron stub.
6. `/health` returns `200`.
7. Unauthorized `/v1/models` returns `401`.
8. Authorized `/v1/models` returns `abdulkarem-ai` alias.
9. API port fallback: when the preferred port is occupied, the gateway starts on the next free port.
10. Rate limit: requests above the configured per-minute limit return `429`.
11. End-to-end mocked Ollama test: general request routes to `abdulkarem-general-sa:v2`.
12. End-to-end mocked OOM test: coding request first selects `qwen3-coder:30b`, receives a simulated KV-cache out-of-memory failure, then falls back to `alenzi-coder-pro-14b:latest` and completes.
13. End-to-end Agent tool test: model requests `read_file`, the real tool reads a workspace file, then Verification reports one successful tool call and a 95% score.

## Not completed in this build container

`npm install` was attempted but the environment timed out before dependencies could be downloaded. Therefore the final Vite/Electron UI build is not claimed as executed here. The Windows installer performs dependency installation on the user's PC.

The user's real Ollama models, GPU/Vulkan memory behavior, and Microsoft Office COM are only testable on the user's Windows environment.
